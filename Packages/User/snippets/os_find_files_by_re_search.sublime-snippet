<snippet>
    <content><![CDATA[
[os.path.join(d, f) for d, ds, fs in os.walk(${1:srcPath}) for f in fs if re.search(re.compile(r"${2:regex}"), f)]
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>os_find_files_by_re_search</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.python</scope>
</snippet>
