<snippet>
	<content><![CDATA[
with open(os.path.join(os.getcwd(), '${1:outFile}.json'), 'w+') as f:
    json.dump(json.dumps(${2:jsonString}, ensure_ascii=False), f)
]]>
	</content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>json_dumps_to_file</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.python</scope>
</snippet>
